// Function to submit the form and refresh the page
        function submitForm(event) {
            event.preventDefault(); 
            // Here, you can add any additional form processing logic if needed.
            // For now, let's just refresh the page:
            location.reload();
        }

        // 